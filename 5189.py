import sys

sys.stdin = open('5189.txt', 'r')


def dfs(i, j, sum_v):
    global min_v
    if sum_v > min_v:
        return
    if i == N:
        print(j)
        sum_v += e[j][0]
        if sum_v < min_v:
            min_v = sum_v
        return
    for k in range(1, N):
        if not visited[k]:
            visited[k] = True
            dfs(i + 1, k, sum_v + e[j][k])
            visited[k] = False

T = int(input())
for test_case in range(1, T + 1):
    N = int(input())
    e = [list(map(int, input().split())) for _ in range(N)]
    visited = [True] * N
    min_v = 0xffff
    dfs(0, 0, 0)
    print(f'#{test_case} {min_v}')





















































































































































































































































































































































































































































































































































































































































































































































